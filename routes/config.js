import express from "express";
const router = express.Router();

/* GET users listing. */
const config = {
    "title":"nocometalworkz",

    "debug":{
        "index":false,
        "scripts":false
    },
    "pause":{

    }
}

export default(config);
